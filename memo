how to run:

nvm use v16.0.0 (nvm alias default 16.0.0)
yarn install & yarn start

replace
  const theme = useAppSelector((state) => state.theme.theme);
to
  const theme = true ? 'light' : 'dark';

local addr:port
  http://172.30.1.15:3000/

to-do
==== 지금

b/e api endpoint 를 f/e 랑 맞추기

core 로직 수정 (getCasesByPackinglistId 실행할때 외띠/cbm 정보 계산하는로직 분리)

2. /packinglists/:id/cases/:cases_id/materials (공주표 페이지)
-> 감가 반영

==== 다음

-> db packinglists table 에 gridId 칼럼 추가. 생성할때 gridID 선택할수있게. 따로 선택안하면 default grid
-> grid relation 이건 따로 없애도 될듯.
-> grid 페이지 (그리드 페이지)

==== 그 다음 
#. grid 탭 생성 (네비게이션 + 페이지 + crud 기능)
#. /dashboard -> / or /recent (통계 이런것보단, google drive 처럼, 최근 생성된 {packinglist, case}, 최근 작업한 {packinglist, case, materials, grid} 이런거)
#. grid 생성/삭제 및 grid 선택 및 적용 (default grid 도 추가) - packinglist 생성할때, grid 선택할수 잇게해야함 (default 포함)
#. 5. 공주표페이지에서 각 재료 row 에 '가감' 기능 추가.
#. 검색 (nav header 에 주석처리한 search 검색기능 및 관련 내용 뽑아내기)
#. 홈 화면 비율 줄였을때 헤더에서 보여질 element 정하기 (email(truncated)----nav bar)
#. t (다국어 지원)
#. 디자인 / 브랜드 입히기 (웹사이트 전체 색상, 제품 로고, 아이콘, 이미지, 404페이지 이미지 등등)
# /Users/user/Desktop/folder/cspark/projects/lightence-ant-design-react-template/src/components/header/layouts/DesktopHeader.tsx
# -> table 만들어서 내보내기 기능 추가하기 (패킹리스트, 케이스, 공주표)
# Quote 기능 (견적서)
